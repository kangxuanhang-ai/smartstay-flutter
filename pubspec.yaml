name: smartstay_flutter
description: SmartStay 智宿云 C端 Flutter App
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0
  flutter: ^3.35.0

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  flutter_bloc: ^9.0.0
  go_router: ^15.0.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
